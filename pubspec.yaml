/ pubspec.yaml (updated)
/*
name: flutter_auth_app
description: A Flutter app with multiple authentication options
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: ^6.1.0
  flutter_facebook_auth: ^5.0.11
  mongo_dart: ^0.9.0
  crypto: ^3.0.3
  provider: ^6.0.5
  http: ^0.13.6
  shared_preferences: ^2.1.1
  flutter_secure_storage: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/
*/
